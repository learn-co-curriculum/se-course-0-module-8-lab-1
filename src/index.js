/* Implement a function that finds the summation of every number from 1 to num. The number will always be a positive integer greater than 0. */
function summation(num) {}

/* Implement a function that finds the mean of a list of numbers. The mean is the sum of all numbers divided by the amount of numbers. */
function mean(nums) {}

/* Implement a function that converts an object into an array, where each element represents a key-value pair in the form of an array. */
// Input: {name: 'Jeremy', age: 24, role: 'Software Engineer'}
// Output: [["age", 24], ["name", "Jeremy"], ["role", "Software Engineer"]]
function convertToArray(obj) {}

/* Implement a function that takes an array of objects with two keys each: state or country, and capital. Keys are strings. */
// Input: [{'state': 'Maine', 'capital': 'Augusta'}]
// Output: ["The capital of Maine is Augusta"]
function capital(capitals) {}

/* Implement a function that finds the sum of all multiples of n below m. n and m are positive integers. */
// example 1: sumMul(2, 9)   ==> 2 + 4 + 6 + 8 = 20
// example 2: sumMul(4, -7)  ==> "INVALID"
function sumMul(n, m) {}

export { summation, mean, convertToArray, capital, sumMul };
